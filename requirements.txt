# 데이터 처리
pandas                 # 데이터프레임 처리 (API/크롤링 데이터 가공)
numpy                  # 수치 계산 및 배열 처리

# API / 웹 데이터 수집
requests               # 공공데이터 API 호출
lxml                   # 빠른 XML/HTML 파서

# 웹 크롤링 (Selenium)
selenium               # 네이버/구글 지도 리뷰 크롤링
webdriver-manager      # 크롬 드라이버 자동 설치 및 버전 관리

#  데이터 시각화
matplotlib             # 기본 시각화
seaborn                # 통계 기반 시각화

# Streamlit 대시보드
streamlit              # 웹 대시보드 UI 프레임워크
folium                 # 지도 시각화 (주차장 위치 표시)
streamlit-folium       # Streamlit에서 folium 지도 안정적으로 렌더링

# 데이터베이스 (MySQL)
mysql-connector-python # MySQL 연결 드라이버
SQLAlchemy             # DB ORM 및 쿼리 관리


#  환경변수 관리
python-dotenv          # .env 파일에서 API KEY, DB 정보 로드

# 유틸리티
tqdm                   # 반복문 진행률 표시 (데이터 수집/전처리)

# 데이터 분석/텍스트 처리
scikit-learn          # TF-IDF, 키워드 분석, 군집화 (리뷰 분석 / 혼잡도 산출)